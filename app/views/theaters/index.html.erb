<div class="col-md-4 col-md-offset-4">
  <h1 align="center">Theaters </h1>
  <%=link_to "Create", new_theater_url,class: "btn btn-success btn-sm" %>
  <%= link_to "Back to Dashboard", dashboard_path, class: "btn btn-info btn-sm" %><br><br>
    <table class="table table-striped">
      <tr>
      <td align="center">Name</td>
      <td align="center">Address</td>
      <td></td>
      <td></td>
      </tr>
    <%@theaters.each do |theater| %>
        <tr>
        <td><%= theater.name %></td>
        <td align="center"> <%= theater.address %></td>
         <td><%=link_to "Edit", edit_theater_path(theater),class: "btn btn-info btn-sm" %></td>
         <td> <%=link_to "Destroy",theater_path(theater),method: :delete, data: {confirm: "Are you sure?"},class: "btn btn-danger btn-sm"%> </td>
        </tr>
    <% end %>
    </table>
</div>

<style type="text/css">
  td
  {
    padding: 10px;
  }
  tr
  {
    padding:
  }
</style>