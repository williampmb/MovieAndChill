<!--TODO: add pagination-->
<div class="container">

	<% @sessions.each do |s| %>

		<table border="1" class="table">
			<tr>
				<th><%= s.movie.title %></th>
			</tr>
			<tr>
				<td>
					Date: 
					<%= s.date.strftime("%b, %d - %Y") %>
				</td>
				
			</tr>
			<tr>
				<td>
					Price:
					$<%= s.price %>
				</td>
			</tr>
			<tr>
				<td>
					<%= link_to "Buy a ticket!", buy_ticket_user_path(current_user.id, session: s.id), method: :post, data: 
	    			{ confirm: "Are you sure you want to buy it?" }  %>
				</td>
			</tr>
			<!--End of table-->
		</table>
	<% end %>
	<%= will_paginate @sessions, inner_window: 2 %>
<!-- End of container-->
<br/>
<%= link_to("Tickets", 
		{:controller => 'tickets',
			:action => 'index'},
			class: "btn btn-primary") %>
<br/>		
</div>